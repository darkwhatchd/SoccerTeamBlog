<div class='container has-background-white'>
  <div class='box'>
    <div class='has-text-centered'>
      <h1 class='title is-4'><%= @notice.title %></h1>
    </div>
    <div class='has-text-left is-size-7'>
      <p>Por: <%= @notice.user.name %></p>
      <%= @notice.publish_at.strftime('%d/%m/%y às %H:%M') %>
    </div>
    <div class='has-text-centered'>
      <%= @notice.body %>
    </div>
    <% if current_user %>
      <a href=<%= "#{edit_notice_path(@notice)}" %> class="button is-dark">Editar</a>
    <% end%>
  </div>
</div>
